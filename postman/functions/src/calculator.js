const sum = (a, b) => {
    return a + b;
}

const diff = (a, b) => {
    return a - b;
}

const product = (a, b) => {
    return a * b;
}

const divide = (a, b) => {
    return a / b;
}

module.exports = {
    sum,
    diff,
    product,
    divide
}
